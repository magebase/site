apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: magebase-app
  namespace: magebase
spec:
  template:
    spec:
      containers:
        - name: magebase
          resources:
            requests:
              memory: '1Gi'
              cpu: '500m'
            limits:
              memory: '2Gi'
              cpu: '1000m'
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: site-prod-cluster-db-url
                  key: DATABASE_URL
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: magebase-ingress
  namespace: magebase
spec:
  tls:
    - hosts:
        - magebase.dev
  rules:
    - host: magebase.dev
